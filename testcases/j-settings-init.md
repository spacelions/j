Prerequisites:
  - Run `make`, make sure ./bin/j is compiled.
  - Make sure `.j` folder doesn't exist.

Steps:
  - `j init` should initialize the `.j` folder
  - `.j` folder contains `settings` binary, `tasks` folder, `tasks/list.db` binary