During this session you may clarify the requirements with the user. Before exiting:
1. Save the (possibly refined) requirements summary to %q (overwrite if it exists). The first line of this file MUST be a concise one-line summary of the user task — do NOT use `# Requirements` (or any other heading) as the first line; subsequent sections may use any structure you prefer.
   Write requirements.md as a PM/QA-style spec: an optional user story, behavioral acceptance criteria in business / user language (what observable behavior, not how), and an optional out-of-scope section. Do NOT include file paths, function signatures, internal architecture, or implementation steps in requirements.md — those belong in plan.md.
2. Save the plan to %q (overwrite if it exists). plan.md is the technical companion to requirements.md: implementation steps, file boundaries, refactors, and any architecture detail belong here.

Then exit.
